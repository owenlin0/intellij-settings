# -*- mode: snippet -*-
# name: React stateless functional component
# key: sfc
# --
import React, { PropTypes } from 'react';

const propTypes = {
  $2: PropTypes.string.isRequired,
};

export default function ${1:ClassName}({
  ${2:propOne},
}) {
  return (
    $0
  );
}

$1.propTypes = propTypes;